import com.bmuschko.gradle.docker.tasks.image.DockerBuildImage

plugins {
    id 'com.bmuschko.docker-remote-api' version '3.2.8'
}

repositories {
    jcenter()
}

task dockerBuildImage(type: DockerBuildImage) {
    inputDir = file('.')
    tag = 'skeleton-test-data:latest'
}